# 引入MQ处理活动SKU库存一致性

## **一、本章诉求**

完成活动责任链判断，包括；活动的校验【日期、状态、sku库存】，之后是sku 库存的扣减操作。 之后这里会涉及一个缓存库存和数据库库存一致性问题，是本节要处理的重点。一个手段是之前在策略实现阶段采用的延迟队列做趋势更新，另外一个是本节要引入 RocketMQ 在库存消耗空以后发送 MQ 消息，直接更新清空最终库存，保持缓存与数据库的一致性。

## **二、功能流程**



完善责任链功能，和库存数据一致性的处理；

![img](https://article-images.zsxq.com/FvvFGEWzmC6q7S_tMgxI3vbymMHJ)

1. 第一步；完成责任链的活动校验，时间、状态、库存。
2. 第二步；对库存的扣减，使用 decr + lock 锁的方式（兜底）进行处理。
3. 第三步；做完库存扣减后，发送延迟队列，由任务调度更新趋势库存，满足最终一致。
4. 第四步；库存消耗为0后，发送MQ消息，驱动变更数据库库存为0

## **三、功能实现**

### **1. 工程结构**

![img](https://article-images.zsxq.com/Fs1QKA8GLty5ojaxHOfJgHDlUct9)

1. 本节引入了 RocketMQ作为消息队列使用。（注意虽然图片是Rabbitmq，但是本次使用rocketmq）
2. domain 领域层是本节要实现的核心主功能，以完善开发上一节的活动责任链功能和扩展出 ISkuStock 接口的能力。
3. trigger 是触发器层，用于接收mq消息、执行任务，和http请求的处理。本节会在 listener 中消费 mq。